rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /pet_photos/{fileName} {
      allow read, write: if request.auth != null && fileName.matches(".*\\.jpg");
    }
  }
}
