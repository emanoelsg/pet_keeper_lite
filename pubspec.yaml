name: pet_keeper_lite
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_messaging: ^16.0.3
  firebase_storage: ^13.0.3
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1
  intl: ^0.20.2
  uuid: ^4.5.1
  image_picker: ^1.2.0
  get: ^4.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
